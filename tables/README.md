# Local-timer finite table

- `local_timer_finite_selected.csv` — representative values of `L=n-1`, exact deterministic BTAI state count `S=n+1`, and `ceil(log2(n+1))` persistent bits.
- `../audit/computational_evidence.py` — wider finite-range generator.

This is a finite theorem audit. It does not extend the result to unrestricted deterministic non-idle-start broadcast and does not claim an exact randomized minimum.
